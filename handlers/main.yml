---
- name: Restart freshclam service
  ansible.builtin.service:
    name: "{{ freshclam_service_name }}"
    state: restarted
  when: clamav_preinstall_signatures

- name: Restart clamd service
  ansible.builtin.service:
    name: "{{ clamav_service_name }}"
    state: restarted
